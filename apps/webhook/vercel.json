{
  "version": 2,
  "workspaceRoot": "../..",
  "buildCommand": "cd ../.. && npm install && npm run build -- --filter=webhook-api",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/webhook/payment",
      "dest": "api/webhook/payment.ts"
    },
    {
      "src": "/api/health",
      "dest": "api/health.ts"
    }
  ]
}
{
  "name": "webhook-api",
  "version": "1.0.0",
  "description": "Payment webhook API for processing transactions",
  "scripts": {
  "dev": "vercel dev",
  "build": "cd ../.. && npm install && npm run build -- --filter=webhook-api && tsc",
  "lint": "eslint .",
  "check-types": "tsc --noEmit",
  "deploy": "vercel deploy --prod"
},
  "dependencies": {
    "@prisma/client": "^6.4.1",
    "@repo/store": "*",
    "@repo/ui": "*",
    "@repo/db": "*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.2.29",
    "typescript": "^5.3.2",
    "vercel": "^41.4.1"
  }
}
